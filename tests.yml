yaml-tests/phpunit: phpunit --version
yaml-tests/phpcs: phpcs --standard=PSR2 -n src --colors
yaml-tests/composer-install:
  command: "composer install --ansi"
CoolTest:
  command: echo "YAAAAAAAAAAAS"
  description: A very important and very cool test.